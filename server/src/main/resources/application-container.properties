# Batch Process Configuration
batch.max_provided_records=${BATCH_MAX_PROVIDED_RECORDS:500}

# Blockapi Configuration
blockapi.uri=${BLOCKAPI_URI}
blockapi.epoch_height=${BLOCKAPI_EPOCH_HEIGHT}
blockapi.enabled=${BLOCKAPI_ENABLED}
blockapi.blockHeightTrackingUuid=${BLOCKAPI_BLOCK_HEIGHT_TRACKING_UUID}

# OS Configration
objectstore.uri=${OBJECTSTORE_URI}
objectstore.private_keys=${OBJECTSTORE_PRIVATE_KEYS}
objectstore.master_key=${OBJECTSTORE_MASTER_KEY}

# Provenance Configuration
provenance.main_net=${PROVENANCE_MAIN_NET}
provenance.chain_id=${PROVENANCE_CHAIN_ID}
provenance.channel_uri=${PROVENANCE_CHANNEL_URI}

# Database
db.type=${DB_TYPE}
db.name=${DB_NAME}
db.username=${DB_USERNAME}
db.password=${DB_PASSWORD}
db.host=${DB_HOST}
db.port=${DB_PORT}
db.schema=${DB_SCHEMA}
db.connectionPoolSize=${DB_CONNECTION_POOL_SIZE}
db.baselineOnMigrate=${DB_BASELINE_ON_MIGRATE:false}
db.repairFlywayChecksums=${DB_REPAIR_FLYWAY_CHECKSUMS:false}
